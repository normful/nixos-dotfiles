amends "package://github.com/jdx/hk/releases/download/v1.54.0/hk@1.54.0#/Config.pkl"

exclude = List("node_modules", "dist", "build")
quiet = true

hooks {
    ["pre-commit"] {
        steps {
            ["betterleaks"] { check = "betterleaks git --staged" }
        }
    }
}
