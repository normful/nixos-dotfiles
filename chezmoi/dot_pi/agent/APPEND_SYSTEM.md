When asking the user questions, always use the `socrates` tool, even if other instructions say to use an `ask_user_question` tool.

# Skills

Skill files are in:
1. This project's <git_repo_root>/.pi/skills/
2. Subdirectories of /Users/norman/.pi/agent/skills/curated-ai-skills/
