local function configure()
  -- TODO: Fix
  -- vim.api.nvim_set_keymap('n', '<Enter>', '<Plug>(LiveEasyAlign)', {})
  -- vim.api.nvim_set_keymap('x', '<Enter>', '<Plug>(LiveEasyAlign)', {})
end

return {
  name = 'junegunn/vim-easy-align',
  disabled = true,
  configure = configure,
}
